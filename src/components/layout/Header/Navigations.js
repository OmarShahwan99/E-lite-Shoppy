import NavigationItem from "./NavigationItems";

const Navigations = (props) => {
  return (
    <ul className="grid grid-cols-1 sm:grid-cols-2 justify-center">
      <NavigationItem />
    </ul>
  );
};

export default Navigations;
